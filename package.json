{
  "name": "sync-package-version",
  "version": "0.0.1",
  "description": "Sync version number from package.json",
  "bin": "lib/cli.js",
  "main": "lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "clean": "rimraf lib dist coverage",
    "prepublish": "npm-run-all clean build",
    "pretest": "npm run build",
    "test": "ava"
  },
  "dependencies": {
    "meow": "^3.7.0",
    "read-pkg": "^2.0.0"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "babel-cli": "^6.16.0",
    "babel-eslint": "^7.0.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-register": "^6.16.3",
    "npm-run-all": "^3.1.0",
    "rimraf": "^2.5.4",
    "temp-write": "^2.1.0"
  },
  "repository": "ratson/sync-package-version",
  "license": "MIT",
  "files": [
    "*.md",
    "LICENSE",
    "lib",
    "src"
  ]
}
